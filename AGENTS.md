## Cursor Cloud specific instructions

This is a newly initialized repository (`ums`) with no application code yet. The repository currently contains only a `README.md`.

### Current state
- **No application code, dependencies, or services** exist in this repository.
- **No build, lint, or test commands** are available.
- There is nothing to start, build, or test at this time.

### When application code is added
- Update this file with instructions for running, testing, and linting the application.
- Set up the appropriate update script via `SetupVmEnvironment` based on the chosen technology stack.
